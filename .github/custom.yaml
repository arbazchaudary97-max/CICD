name: CoderCo Custom Action CI


on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v4

    -name: Hello world custom action 
    uses:  arbazchaudary97-max/coderco-custom@main
    with:
        who-to-greet: "CoderCo Team"